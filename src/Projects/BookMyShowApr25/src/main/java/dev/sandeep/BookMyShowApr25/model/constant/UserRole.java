package dev.sandeep.BookMyShowApr25.model.constant;

public enum UserRole {
    CUSTOMER, ADMIN, ANONYMOUS;
}

//customer -> logged in user
// anonymous -> non-logged in user
// admin -> user with special privileges
